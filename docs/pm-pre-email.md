Subject: Quick review — sprint review process draft before I send to the team

Hey [PM name],

I put together a draft for how we can run sprint reviews going forward. It covers the DoD we discussed, a simple meeting flow (demo then verification), and a script I built that checks our PRs against the DoD automatically using GitHub and SonarCloud data.

Before I send it to the team I wanted to get your take:

- Does the DoD look right based on what we talked about? I have 4 checks right now — code reviewed, tests passing, merged to main, SonarCloud clean. Open to adding or changing anything.
- The meeting flow is demo first (~30 min), then I run through the DoD report (~10 min), then we accept/reject together (~5 min). Does that work for you?
- Anything you'd want framed differently or anything I'm missing?

No rush — just want to make sure we're aligned before it goes out. Happy to jump on a quick call if that's easier.

Thanks,
James
